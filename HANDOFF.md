# 引き継ぎドキュメント（2026-07-15 更新）

新しいセッション・別モデルで作業を再開するときは、まずこのファイルを読むこと。
作業を進めたら **このファイルの「未処理事項」を必ず更新** すること。

## プロジェクト概要

お寺の空きスペース・空き時間 × イベント企画者マッチングプラットフォーム
「テラつなぎ（仮称）」。学生起業チームによる新規事業。

- **現在フェーズ: 企画・プロトタイプ段階**（本開発の実装コードはまだ無い）
- 要件定義・システム設計書 → `DESIGN.md`（v1・スペース貸出）／
  **`DESIGN_V2_PORTAL.md`（v2・スペース貸出×お寺特化スキマバイトの統合ポータル。全体マップ＋拡張DB）**
- **デザインシステム（実装の型・正）→ `DESIGN_SYSTEM.md`** ＋ 目視用スタイルガイド `prototype/design-system.html`
  / https://claude.ai/code/artifact/0de2420e-de7f-457f-9cba-725d70c7f8c7
- プロトタイプ（外部依存ゼロの自己完結 HTML/CSS/バニラJS。同ファイルパスで再公開すればURL維持、別会話からは `url` 指定）:
  - 寺院向けダッシュボード → `prototype/temple-owner-dashboard.html`
    / https://claude.ai/code/artifact/4f4e53f0-acf7-4106-81c5-df488c1edd80
  - 空き枠 新規掲載フォーム（4ステップ・ライブプレビュー付き・場所は寺院ごとに追加登録可）→ `prototype/temple-listing-form.html`
    / https://claude.ai/code/artifact/982f9a62-56bd-4c3d-860b-17376fd4f3b1
  - 申込詳細／相談スレッド（寺・企画者・運営の三者・クイック返信・承認/辞退）→ `prototype/temple-application-thread.html`
    / https://claude.ai/code/artifact/7b32c3a2-c23e-4014-9a70-eb3dccf439f4
  - 企画者側 空き枠検索〜申込（検索・絞り込み＋スライドオーバー申込フォーム）→ `prototype/organizer-search.html`
    / https://claude.ai/code/artifact/c645b5d1-43b1-4b08-874c-671328809d4f
  - 公開LP（お寺入口／企画者入口の二面・各画面へ実リンク）→ `prototype/landing.html`
    / https://claude.ai/code/artifact/b60cb1ff-9d6d-44ec-a159-c6f5507f25b0
  - **【v2】統合ポータル（画面A ゲートウェイ＋画面B お寺統合ダッシュボード、トグル切替）→ `prototype/portal.html`
    / https://claude.ai/code/artifact/1c8518d9-ac0b-4ab3-926e-d11d66684db7**
    - 画面A：一般ユーザーを「①スペース借用（抹茶）」「②お寺スキマバイト（朱）」の2入口にスプリット分岐＋お寺関係者ログイン導線
    - 画面B：左ナビ「ホーム／スペース貸出管理／スキマバイト管理」の統合DB。ホームに本日のお知らせ（イベント申込・バイト応募の件数）
      と直近スケジュール（スペース利用予定／本日来る学生）。スペース承認・バイト採用/面談/見送りが動作
    - スキマバイト機能（`gigs`/`gig_applications`、users統合）は `DESIGN_V2_PORTAL.md` 参照。**既存のスペース系画面と統合が必要**
- **プロトタイプは主要5画面が揃い、掲載→検索→申込→相談→承認の往復が通して見える状態**（LPの各CTAは
  上記の実Artifact URLへリンク済み）
- チーム共有用インデックス（全画面・資料・進捗のハブ）→ `prototype/index.html`
  / https://claude.ai/code/artifact/532711ff-3770-4523-a139-fb2f522e1dd1
- **Git 管理開始済み**（2026-07-15 初回コミット）。プロトタイプの公開デプロイ設定も投入済み（下記「デプロイ」）
- 本開発の技術スタック（Next.js等）はまだ確定していない（下記「未処理事項 B-1」で決める）。
  ※ 現状の公開は「プロトタイプHTMLの静的配信」であって、本アプリの実装ではない点に注意

## デプロイ（プロトタイプの公開・Cloudflare／お墓サポートと同方式）

- `wrangler.toml` … `[assets] directory = "./prototype"` で静的配信。**公開トップ＝`prototype/index.html`**
- 公開サイト内で完結するよう、`index.html`/`landing.html` の内部リンクは claude.ai ではなく
  ローカルHTML（`temple-owner-dashboard.html` 等）へ張替え済み
- **未完了（ユーザー操作が必要）**: GitHubに空リポジトリ `tera-tsunagi`(kkishi0724-os) を作成 → push →
  Cloudflare で当リポジトリを連携。以降は `git push` で自動デプロイ
- リポジトリ内の各Artifact公開URL（claude.ai）は claude.ai 上の別コピー。公開サイトとは別物として併存

## モデルの使い分け方針（重要・ユーザー指示 2026-07-15）

**下位プランで運用中。上位モデル（フェイブル5 等）が使える時間は限られる。**
そこで「フェイブル5 でこそやるべきこと＝人間的な判断・デザイン・設計の"型"決め」を
**先に片付ける**。型（デザインシステム・画面仕様・DBスキーマ・実装パターン）が固まれば、
以降の反復実装は下位モデルでも安全に回せる。

作業を選ぶときは、下の「A: 上位モデル優先」を上から消化し、
「B: 下位モデルでも可」は型が決まってから着手すること。

---

## 未処理事項

### A. 上位モデル（フェイブル5）で先にやるべきこと ＝ 最優先

判断・創造・曖昧さの解消が要るもの。ここを固めきるほど、後工程を下位モデルに渡せる。

1. **[済 2026-07-15] A-1 デザインシステムの確定** → `DESIGN_SYSTEM.md`（正・ソースオブトゥルース）
   ＋ ビジュアル・スタイルガイド `prototype/design-system.html`
   / https://claude.ai/code/artifact/0de2420e-de7f-457f-9cba-725d70c7f8c7 。
   全カラートークン（light/dark確定値）・タイポ（明朝/ゴシック・型スケール・17px寺院/16px公開のルール）・
   余白/角丸/影・テーマ切替・部品カタログ（18種＋参照実装）・実装マッピング（globals.css一元化＋Tailwind
   theme.extend＋Reactコンポーネント名＋DBスキーマ対応）・裁定メモ（washi色味/フォントサイズ/角丸のぶれを統一）。
   **これで B 系の実装は判断せず DESIGN_SYSTEM.md に沿って埋めるだけにできる。**
2. **A-2 未実装の主要画面のUX設計**（ワイヤー＋主要コピー）。
   - [済 2026-07-15] 空き枠 新規掲載フォーム → `prototype/temple-listing-form.html`。
     4ステップのウィザード（①場所②日時③条件・設備④確認）＋「企画者にはこう見えます」ライブプレビュー。
     入力より選択（選択カード／プリセットチップ／＋−ステッパー／トグル）でシニアの負担を最小化する型を確立。
     **ステップ①の場所は寺院ごとに異なるため、既存の登録場所からの選択に加え「別の場所を追加」で
     アイコン・名前・説明を自由に登録できる**（ユーザー要望 2026-07-15）。実装では登録場所を
     `spaces` テーブル（DESIGN.md）から取得し、その場での新規追加も同テーブルへ保存する想定。
   - [済 2026-07-15] 申込詳細／相談スレッド → `prototype/temple-application-thread.html`。
     左に申込詳細（sticky・承認/辞退アクション）、右に三者スレッド。**寺=右・抹茶／企画者=左・中立／
     運営=全幅・金茶の帯**で描き分け、「運営が仲介・包括請負」を可視化。シニア向けにクイック返信チップを用意。
     実装では `messages`（sender_type=寺|企画者|運営）と `applications.status` に対応。
   - [済 2026-07-15] 企画者側: 空き枠検索〜申込フォーム → `prototype/organizer-search.html`。
     キーワード検索＋企画ジャンル/場所タイプの絞り込み（`spaces`/`availabilities` を横断検索する想定）、
     カードの「申し込む」からスライドオーバーで申込フォーム（企画タイトル・ジャンル・希望日時選択・想定人数・
     企画内容）。送信で `applications` を作成 → 寺院ダッシュボードの「新着の申込」＋相談スレッドへ繋がる導線。
     モバイル前提の回遊UI（企画者は若手中心）だが、寺院側と同じトークンで一貫。
   - [済 2026-07-15] 公開LP（お寺入口／企画者入口の二面）→ `prototype/landing.html`。
     エディトリアル方針で一段強めた表現：主見出し「お寺の敷居を、そっと下げる。」、縦書きタグライン、
     禅の円相（ensō）SVG一筆書きアニメ、落款スタンプ、スクロールリビール。二つの入口カード・使い方4ステップ
     （一二三四）・生まれた企画・運営サポート帯・最終CTA。各CTAは寺院ダッシュボード/企画者検索の実Artifact URLへリンク。
     → **A-2 は全画面完了。**
3. **A-3 サービス名・ブランドの確定**（現状は仮称「テラつなぎ」）とロゴ・トーンの言語化。
   - [たたき台済 2026-07-15] → `A3_BRAND.md`。ポジショニング・タグライン候補・名称候補6案（評価軸つき）・
     トーン&ボイス・ロゴ方向。**推奨は「テラつなぎ」または「てらひらき」に絞り、商標(J-PlatPat)・ドメイン空きを
     確認して最終決定**（空き確認は未実施）。決定後に全プロトタイプ/規約/READMEの「テラつなぎ（仮称）」表記を一括更新。
4. **A-4 プロダクト判断**: 課金/手数料モデル、審査フロー、キャンセル・トラブル時の
   運営介入ルールなど、正解が一つでない意思決定。
   - [たたき台済 2026-07-15] → `A4_PRODUCT_DECISIONS.md`。論点ごとに選択肢＋推奨デフォルト＋
     事業/DB/画面への影響を整理。6章に「会議で埋める決定サマリー表」あり。
     **状態は全論点『要決定』＝チームで採用/修正を記入する段階**。課金は成立手数料10〜15%(企画者負担)＋
     サポート課金＋外部決済委譲を推奨。免責/保険/決済/宗教施設の規約は専門家確認が必須と明記。
   - [たたき台済 2026-07-15] 利用規約・企画ガイドライン・免責/保険案内のドラフト → `LEGAL_DRAFTS.md`。
     A-4未確定の数字は `[〜]` プレースホルダ。**弁護士・保険代理店レビュー必須**と明記。
   - [次] 上表をチームで確定 → `[〜]` を埋める → DESIGN.md スキーマへ反映（`fees`/`support_orders`/
     status に`キャンセル`追加 等）→ プライバシーポリシー作成 → 専門家レビュー → サイト掲載。

### B. 下位モデルでも安全にできること ＝ 型が決まってから

A で"型"が決まっていれば、パターン反復・定型実装なので下位モデルに任せてよい。

1. **[済 2026-07-15] B-1 技術スタック選定＋初期セットアップ** → `web/`（実アプリ）。
   他プロジェクト踏襲で **Next 16.2.7 / React 19.2.4 / Tailwind v4 / TS5 / 静的export**。
   **`web/` サブフォルダに配置**（ルートの `wrangler.toml` が `./prototype` を配信中で公開サイトが
   稼働しているため、それを壊さず並走）。手順・切替方針は `B_IMPLEMENTATION.md`、Next16注意は `web/AGENTS.md`。
   - `app/globals.css` に **DESIGN_SYSTEM.md のトークンを Tailwind v4 `@theme inline`＋CSS変数で実装済み**
     （`bg-washi`/`text-sumi`/`bg-matcha-bg`/`font-serif`、ライト/ダーク対応）。
   - `components/Button.tsx`・`StatusPill.tsx` を**移植パターン見本**として配置。`app/page.tsx` は動作確認デモ。
   - **検証済み**: `cd web && npm install && npm run build` 成功（TypeScript通過・静的export・生成CSSにトークン反映）。
2. **[次] B-2 各画面のコーディング**（型に沿った量産）。`B_IMPLEMENTATION.md` の推奨順に、
   共通部品→LP→ダッシュボード→掲載→スレッド→企画者検索、を `prototype/*.html` から移植。
3. **B-3 DBスキーマ（DESIGN.md 5章）のマイグレーション実装**（A-4決定を反映：`fees`/`support_orders`/
   status に`キャンセル`追加）。まずはモックデータで画面を作り、後でDB接続。
4. **B-4 公開の切替**（実アプリが揃ったら `web/out` を Cloudflare へ。案A/B は `B_IMPLEMENTATION.md`）。

---

## 注意事項

- プロトタイプは Artifact のCSP制約に合わせ **CDN不使用・全インライン**。CJKフォントは
  データURI埋め込みを避け、明朝＝游明朝/ヒラギノ明朝、ゴシック＝游ゴシック/メイリオの
  システムフォントで表示している（Windows で確実に出る）。
- ターゲットにシニア層（ご住職）を含むため、可読性・タップ領域・平易な日本語を優先する方針。
- 本プロジェクトのアカウント類・デプロイ先はまだ未確定（他プロジェクトは
  kansaiohakasupport@gmail.com / GitHub kkishi0724-os で運用）。
- 学生チームの「端末内データのみの低リスク設計」志向（team-stats で確定した方針）が
  この案件にも及ぶかは A-4 で要確認。
